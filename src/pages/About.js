import React from "react";
import styled from "styled-components";

const AboutBox = styled.div`
  height: 100vh;
`

function About() {
  return <AboutBox>You are currently on the About Page</AboutBox>;
}

export default About;
